# Ordering

You can define the ordering of pages in an index by creating an `index.txt` file in your folder. The `index.txt` file must have the names (without extensions) of the pages in that folder, indicating the order.

### Example

For a folder, the `index.txt` might look like this:

```
adding-pages
ordering
contents
context
building
```