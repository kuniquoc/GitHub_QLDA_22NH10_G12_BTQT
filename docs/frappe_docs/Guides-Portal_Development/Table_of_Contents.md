# Table of Contents

You can add a table of contents by adding the `{index}` string on a new line. This works only in `index.md` or `index.html` files.

## Example

```markdown
# Contents
{index}
```

This will render as a numbered list of pages.